<%= form_for @difficulty_vote, remote: true, html: {class: 'form-difficulty'} do |form| %>
	<div class="">
		<% (1..5).each do |v| %>
			<div class="radio-inline">
				<%= form.radio_button :note, v %>
				<%= v %>
			</div>
		<% end %>
		<%= form.hidden_field :utauloid_id %>
		<%= form.submit "Vote", class: 'btn btn-success' %>
	</div>
<% end %>