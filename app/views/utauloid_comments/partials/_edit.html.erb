<%= form_for @comment, remote: true, html: {class: 'form-comment'} do |form| %>
	<%= form.text_field :message, class: 'form-control', required: true %>
	<%= form.submit "Done", class: 'btn btn-success' %>
<% end %>
