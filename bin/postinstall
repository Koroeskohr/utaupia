#!/usr/bin/env ruby

system 'echo "gem: --no-document" > ~/.gemrc'
puts 'installing rails'
system 'gem install rails'
system 'gem install bundler'
puts 'rbenv rehash'
system 'rbenv rehash'
system 'bundle install'
puts 'Imagemagick'
system 'sudo apt-get install imagemagick'